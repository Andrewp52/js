const str1 = 'Hello';
const str2 = 'world';
const name = 'Andrew'

const concatenated = str1 + ' ' + str2 + ' i\`m ' + name; // simple concat

const concatWithTempl = `${str1} ${str2} i\`m ${name}` // concat with template

console.log(concatenated);
console.log(concatWithTempl);



